/* AKSI Counterfactual Check — surfaces assumptions and failure conditions. */
(()=>{'use strict';function analyze(answer,source){const text=String(answer||''),src=String(source||'');const assumptions=[];if(!src)assumptions.push('Источник отсутствует или не указан');if(/всегда|никогда|точно|гарантированно/i.test(text))assumptions.push('Формулировка содержит абсолютное утверждение');if(/вероят|может|возможно/i.test(text))assumptions.push('Результат зависит от неопределённости');if(!assumptions.length)assumptions.push('Явных слабых мест по простым эвристикам не найдено');return{schema:'AKSI-COUNTERFACTUAL-1',failure_conditions:assumptions,not_proof:true,note:'Это эвристический анализ. Он не заменяет независимую проверку.'}}window.AKSICounterfactual={analyze}})();
