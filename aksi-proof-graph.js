/* AKSI Proof Graph — explicit evidence structure, never hidden chain-of-thought. */
(()=>{'use strict';const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function build({question='',answer='',source='',status='unverified',assumptions=[],counterEvidence=[]}={}){return{schema:'AKSI-PROOF-GRAPH-1',nodes:[{id:'q',type:'question',text:question},{id:'c',type:'claim',text:answer},{id:'e',type:'evidence',text:source||'Источник не указан',status:status},{id:'a',type:'assumption',text:assumptions.length?assumptions.join('; '):'Явные допущения не указаны'},{id:'x',type:'counter-evidence',text:counterEvidence.length?counterEvidence.join('; '):'Контрдоказательства не проверялись'}],edges:[['q','c'],['e','c'],['a','c'],['x','c']],limits:['Граф показывает проверяемую структуру результата, а не скрытые рассуждения модели.','Evidence не означает автоматически truth.']}}function render(g,host){if(!host)return;host.innerHTML='<div class="pg-head"><b>PROOF GRAPH</b><span>Проверяемая структура результата</span></div><div class="pg-grid">'+g.nodes.map(n=>'<article class="pg-node pg-'+esc(n.type)+'"><small>'+esc(n.type)+'</small><strong>'+esc(n.text)+'</strong>'+(n.status?'<em>'+esc(n.status)+'</em>':'')+'</article>').join('')+'</div><p class="pg-limit">'+esc(g.limits[0])+'</p>'}window.AKSIProofGraph={build,render}})();
