name: AKSI Trust Core

on:
  pull_request:
  push:
    branches: [main, 'aksi-product-ready-*']

permissions:
  contents: read

jobs:
  trust:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install backend dependencies
        run: pip install -r requirements.txt
      - name: Compile backend
        run: python -m compileall -q .
      - name: Run trust and release contract tests
        run: python -m pytest tests/test_trust.py tests/test_release_contract.py -q
