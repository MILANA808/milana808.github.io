(()=>{'use strict';
/* AKSI PRECEDENT ENGINE — a model-agnostic truth/decision layer.
   It never upgrades guesses into facts. It separates claim, evidence, inference,
   uncertainty and decision, and produces a reproducible local receipt. */
const KEY='AKSI_PRECEDENT_V1';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const sha=async text=>{if(crypto?.subtle){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}return 'local-hash-unavailable'};
const load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}};
const save=x=>{try{localStorage.setItem(KEY,JSON.stringify(x.slice(-50)))}catch{}};
function mount(){if(document.querySelector('[data-precedent-root]'))return;const host=document.querySelector('[data-view="home"]');if(!host)return;
const box=document.createElement('section');box.className='precedent';box.dataset.precedentRoot='1';box.innerHTML=`<div class="precedent-head"><div><div class="eyebrow">AKSI PRECEDENT ENGINE</div><h2>Не «самый умный ответ». Самый проверяемый.</h2><p>Один слой поверх любой модели: отделяет факт от предположения, показывает неизвестное, предлагает проверку и оставляет воспроизводимый след.</p></div><div class="precedent-badge">TRUTH<br><b>FIRST</b></div></div><div class="precedent-grid"><div><label>Что нужно понять или решить?</label><textarea id="precedentInput" placeholder="Например: «Стоит ли запускать этот продукт?» или «Правда ли, что…?»"></textarea><button id="precedentRun" class="precedent-run">Разложить по полочкам →</button></div><div id="precedentResult" class="precedent-result"><div class="precedent-empty">Напиши один вопрос. АКСИ покажет не только ответ, но и <b>что известно, чего не хватает и как это проверить.</b></div></div></div><div class="precedent-principles"><span>01 · CLAIM</span><span>02 · EVIDENCE</span><span>03 · INFERENCE</span><span>04 · UNCERTAINTY</span><span>05 · CHECK</span><span>06 · DECISION</span></div>`;host.appendChild(box);
box.querySelector('#precedentRun').onclick=run;box.querySelector('#precedentInput').addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter')run()});
}
async function run(){const input=document.getElementById('precedentInput')?.value.trim();const out=document.getElementById('precedentResult');if(!input||!out)return;const lower=input.toLowerCase();const looksFact=/^правда ли|^верно ли|^действительно ли|^is |^are |^does |^can /i.test(input);const needsEvidence=/правда|факт|доказ|точно|гарант|всегда|никогда|лучший|самый|будет|стоит|можно ли|опасно|безопас/i.test(lower);const receipt={question:input,at:new Date().toISOString(),mode:'local-first',classification:looksFact||needsEvidence?'CLAIM_REQUIRES_EVIDENCE':'DECISION_REQUIRES_CRITERIA'};receipt.hash=await sha(JSON.stringify(receipt));const log=load();log.push(receipt);save(log);
const cls=looksFact||needsEvidence?'ТРЕБУЕТ ДОКАЗАТЕЛЬСТВА':'ТРЕБУЕТ КРИТЕРИЕВ';const confidence=looksFact||needsEvidence?'Нельзя честно назвать это фактом без источника или наблюдения.':'Недостаточно данных для честного «да/нет» — сначала нужны критерии и ограничения.';out.innerHTML=`<div class="receipt"><div class="receipt-top"><span class="status-chip">${cls}</span><span class="mono">${esc(receipt.hash.slice(0,16))}…</span></div><div class="receipt-row"><b>Вопрос</b><span>${esc(input)}</span></div><div class="receipt-row"><b>Что известно</b><span>Сам вопрос зафиксирован. Внешние факты здесь не придуманы и не подменены вероятностью.</span></div><div class="receipt-row"><b>Что неизвестно</b><span>${esc(confidence)}</span></div><div class="receipt-row"><b>Следующий шаг</b><span>${looksFact||needsEvidence?'Добавить источник, измерение, эксперимент или первичный документ.':'Задать 3–5 критериев успеха, ограничения, цену ошибки и допустимый риск.'}</span></div><div class="receipt-row"><b>Контрпроверка</b><span>${looksFact||needsEvidence?'Попробовать опровергнуть утверждение независимым источником.':'Сформулировать сильнейший аргумент против выбранного решения и проверить его отдельно.'}</span></div><div class="receipt-foot">LOCAL RECEIPT · SHA-256 · ${esc(receipt.at)}</div></div>`;window.dispatchEvent(new CustomEvent('aksi:precedent',{detail:receipt}));}
window.AKSIPrecedent={version:'1.0.0',run,mount,history:load};document.addEventListener('DOMContentLoaded',mount);})();
